# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database Configuration (if using direct database access)
DATABASE_URL=your-connection-string

# Authentication Configuration
NEXT_PUBLIC_AUTH_REDIRECT_URL=http://localhost:3000/auth/callback
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: Email Service Configuration (if using email notifications)
EMAIL_SERVICE_API_KEY=your-email-service-key
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Optional: Storage Configuration (if using Supabase Storage)
NEXT_PUBLIC_STORAGE_BUCKET=your-bucket-name 